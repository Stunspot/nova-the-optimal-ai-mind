# Current release record

Status: approved current design.

The release uses tenant-scoped retrieval indexes and performs authorization before retrieval. This record supersedes `old-design.md`, whose shared-index proposal is retained only as historical evidence.

Any future change to the tenant boundary requires a security review and fresh denial-path verification.
