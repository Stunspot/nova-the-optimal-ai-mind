# Authorization and steering demonstration

This example starts before full execution authority. Dennis keeps the plan provisional while making the governing decisions visible.

The record separates the intended customer outcome from the proposed intervention, records credible alternatives, leaves an unknown benefit baseline unknown, and assigns a measurement task. Its 8–15 working-day range is a low-confidence authorization-probe forecast, not a launch promise. Capacity names billing review as the bottleneck, limits WIP, and shows the queue. The stakeholder state records evidence and a next move without inventing consent.

Run:

```powershell
python scripts/project_control.py validate examples/authorization-and-steering/project-control.json
python scripts/project_control.py status examples/authorization-and-steering/project-control.json
```

A valid record proves declared-state integrity only. The project remains proposed until its named authority approves the gate.
