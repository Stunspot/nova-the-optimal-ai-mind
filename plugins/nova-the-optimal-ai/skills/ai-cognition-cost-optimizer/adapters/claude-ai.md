# Claude.ai Adapter

Upload the complete one-skill ZIP through **Customize > Skills > + > Create skill**. The cost-accounting, comparison, templates, references, and chat fallback remain usable when Claude's code environment can run the included standard-library scripts.

Claude.ai does not normally share the user's localhost network, so it should not claim that it inventoried or executed against the user's Ollama runtime. Preserve the local route as a plan, name the lost local-execution guarantee, and hand the supplied policy and request to Claude Code, Codex, or another authorized host on the Ollama machine.
