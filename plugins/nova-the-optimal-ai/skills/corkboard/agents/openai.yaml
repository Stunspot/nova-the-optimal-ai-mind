interface:
  display_name: "Corkboard"
  short_description: "📌 Reminder RAG for contextual resurfacing."
  default_prompt: "Use $corkboard to front-load this pin with retrieval-useful concepts and resurface it through contextual RAG."

policy:
  allow_implicit_invocation: true
