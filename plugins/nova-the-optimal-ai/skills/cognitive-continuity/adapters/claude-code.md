# Claude-compatible adapter for Nova the Optimal AI Free

Install the complete Claude-compatible Nova the Optimal AI bundle through the product-level START-HERE.md, or install the complete set of per-skill folders through the host's supported Agent Skills route. Do not flatten Cognitive Continuity into a prompt fragment.

For Nova-owned state, use the sibling nova-operations registry-backed launcher. It injects the customer-approved NOVA_CONTINUITY_HOME and refuses host .codex custody. Agent DREAMing is a nested MIND Core inside Nova, not a separate sibling skill.

Claude host conventions may change. Verify current discovery, permissions, and one real invocation before calling the installation active. Package structure alone establishes none of those host observations.
