interface:
  display_name: "Agent Striving"
  short_description: "🏹 Durable objective continuity."
  default_prompt: "Use $agent-striving to keep this authorized pursuit alive across changing work and interruption, recover its current thread, make the next fitting move, and rest without manufacturing proof or ceremony."
policy:
  allow_implicit_invocation: true
