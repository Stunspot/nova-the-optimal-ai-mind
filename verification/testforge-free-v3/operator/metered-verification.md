# Metered verification — HOLD_UNKNOWN

## Capacity

Provider: GitHub Actions. Billing scope, observation time and validity, remaining included allowance, principal-set reserve, provider multiplier, and paid-overage availability are all `unknown`. The canonical assessor was not invoked because its required authoritative snapshot fields were unavailable; inventing them would manufacture permission-shaped evidence.

## Expansion

Verification: `1 trigger × 3 jobs × 1 attempt × 15 ceiling minutes = 45 raw runner-minutes`; plus `1 trigger × 1 job × 1 attempt × 5 ceiling minutes = 5 raw runner-minutes`; total `50 raw runner-minutes × unknown provider multiplier = unknown estimated billed minutes`.

Separate publication inventory: `1 trigger × 1 job × 1 attempt × 10 ceiling minutes = 10 raw runner-minutes`. If separately proposed with verification, the complete inventory would be `60 raw runner-minutes × unknown provider multiplier = unknown estimated billed minutes`.

## Decision

**HOLD_UNKNOWN.** Automatic invocation is false. Paid dispatch is false. Hosted runs are zero. No workflow was launched merely to discover capacity.

## Substitute

**EXECUTED:** 36-test local suite, deterministic clean-checkpoint build, exact qualification-package verifier, documentation and site checkers, and frozen-source checks.

`PREPARED — NOT EXECUTED: clean-host or self-hosted cross-platform verification, pending suitable hosts and authority.`

`This substitute does not prove: provider runner/image behavior; provider trigger/matrix behavior; permission/secret integration; artifact integration; status integration; Pages deployment or public availability.`

## Authority

Before proposing any hosted dispatch, provide a fresh authoritative observation naming the exact GitHub billing scope, observation time and validity, remaining included allowance, principal-set reserve, current provider multiplier, and paid-overage availability. No hosted, paid, or publication action is authorized. `deploy-pages` is not candidate verification.