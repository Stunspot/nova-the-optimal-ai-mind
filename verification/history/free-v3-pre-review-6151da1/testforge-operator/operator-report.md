# TestForge operator report — Nova the Optimal AI Free 3.0.0

## Decision

**Status:** `NOT_READY` for public redistribution.

**Local qualification:** `PASS_WITH_EXPLICIT_EVIDENCE_BOUNDARIES` for source composition, deterministic package construction, core state behavior, adversarial package checks, maintained documentation, and the static site on the exercised Windows host.

**Target:** source checkpoint `6151da1862c93e2af1755a5b79aee239816c5d1f` plus the final rebuilt customer package. The builder recorded that checkpoint as clean. E-005 confirms no product/source path outside `verification/` differs from it; later dirt is verification evidence only.

**Hesperos disposition:** `REVIEW_PASS_WITH_CONDITIONS`. The source and rebuilt customer package have no remaining material documentation finding. Conditions remain for assistive-technology and representative-user testing, broader mobile/zoom coverage, external-link availability, and fresh-host/live routes.

**TestForge reviewer disposition:** `NOT_RUN`. This operator packet is now stable and ready for a fresh independent `$verification-reviewer` context.

The six missing component grants are release-blocking regardless of the green local evidence. TestForge therefore returns a successful verification process and a failed public-release ordeal. No archive digest in builder-owned records is promoted here into a TestForge release seal.

## Scope and claim boundary

This cycle covered the one-plugin/25-root/16-core topology, source custody, protected-root integrity, deterministic and cross-host package parity, package inventories, optional estate behavior, documentation and site journeys, redistribution-state enforcement, and the GitHub Actions metered preflight.

It did not cover a push, tag, release, deployment, submission, distribution, or announcement; clean-host discovery and invocation; GitHub-hosted execution; or semantic outcome efficacy for every specialist. Static/package evidence is not fresh-host or user-outcome evidence.

## Change impact and critical invariants

The redesign replaces the old two-plugin/runtime architecture with one `nova-the-optimal-ai` plugin, one `$nova` front door, 25 visible skill roots, and MIND 0.2.0 with 16 nested Faculty Cores. Ordinary use has no Python, Ollama, embedding, vector-database, hook, daemon, or estate requirement. Optional state is governed by Nova Operations and four Free selectors outside `.codex`.

The critical invariants are exact topology, locked persona/import trees, unchanged protected Agent Swarm/TestForge roots, deterministic archives, Codex/Claude skill-byte parity, external state custody with atomic failure behavior, source/package documentation parity, preserved redistribution blockers, and separate authority for consequential external actions.

## Risk-ranked strategy

Critical topology and custody risks were tested at the source-contract and extracted-package layers. Build and host-parity risks were tested through two temporary builds plus the final package oracle. State-custody risks were tested as observable state transitions and negative cases, including failed init and forbidden fallback. Documentation drift received source lint, a deployed-artifact boundary regression, an 11-page site check, extracted-package reference checks, rendered inspection, task walkthrough, and independent Hesperos review. The license gate was audited semantically; the result is blocking rather than green.

The canonical manifest records 12 risks, 11 scenarios, 14 tests, and 7 executions. Both TestForge validators pass with no errors or warnings.

## Checks actually performed

| Execution | Exact command | Result | Time | Raw evidence |
|---|---|---|---:|---|
| E-001 | `python -B -X utf8 -m unittest discover -s tests -v` | PASS — 22 tests | 137.641 s | `raw/full-unittest.json` |
| E-002 | `python -B -X utf8 tools/verify_package.py dist/nova-the-optimal-ai-free-3.0.0` | PASS — 25 roots, 16 cores, 131 customer references | 6.285 s | `raw/package-verifier.json` |
| E-003 | `python -B -X utf8 tools/check_documentation.py` | PASS — 16 documents, 13 local references | 0.122 s | `raw/documentation-check.json` |
| E-004 | `python -B -X utf8 docs/check_site.py` | PASS — 11 pages | 0.124 s | `raw/site-check.json` |
| E-005 | `git diff --exit-code 6151da1862c93e2af1755a5b79aee239816c5d1f -- . :(exclude)verification` | PASS — no non-verification path differs | bounded local | `raw/source-checkpoint-diff.json` |
| E-006 | Clean-host install/discovery/restart/invocation/rollback | NOT RUN | — | none |
| E-007 | Manual GitHub Actions three-OS `workflow_dispatch` | NOT RUN — `HOLD_UNKNOWN` | — | `metered-verification.md` |

The repository's official validator receipt additionally records 25/25 skill validators and the plugin validator passing in Python UTF-8 mode. It is supplemental evidence because this operator did not retain the exact per-root batch command in its own command recorder.

The heuristic test-smell scan found no warnings across the five top-level regression files; it is not treated as a correctness oracle.

## Findings and required fixes

`F-001` is critical. Nova/MIND, Nova Operations, AnswerLayer, and Current Intelligence Observatory need explicit Free-edition grants. Job Application Builder and Interview Trainer expressly prohibit inclusion without separate written permission. AnswerLayer and Current Intelligence metadata must then be reconciled. Until those steps are evidenced, `PUBLIC_REDISTRIBUTION_BLOCKED` is the only defensible state.

`F-002` is high. No disposable clean Codex or Claude-compatible host has installed, discovered, restarted, invoked, and rolled back this exact package.

`F-003` is medium. GitHub-hosted runner, matrix, permission, artifact, and status integration are unobserved. Current private-repository capacity and billing scope are unknown. The 3-OS verification workflow expands to 45 raw runner-minutes; the Pages workflow has a 10-minute ceiling but is also an unauthorized publication action. Neither ran.

`F-004` is medium. The package cycle establishes composition and selected mechanisms, not semantic efficacy or user outcomes for all 25 roots.

## Residual risk and unperformed checks

Hesperos conditions remain for screen-reader behavior, keyboard-only use, broader mobile and zoom views, external-link availability, and representative-user task completion. Fresh-host execution remains a release gate. A clean or self-hosted three-OS substitute could add platform evidence, but it would not prove GitHub's runner images, trigger/matrix wiring, permissions/secrets, artifact integration, Pages behavior, or check/status integration.

The source-lock and source-map are treated as maintained custody evidence; this operator did not refresh every upstream remote. Any later source or documentation change voids the current cutoff and requires a new bounded cycle.

## Reproduction and next actions

Reproduce the local decision-critical evidence with the five executed commands above. Validate the packet with:

```text
python -B -X utf8 plugins/nova-the-optimal-ai/skills/software-verification/scripts/validate_manifest.py verification/testforge-free-v3/operator/verification-manifest.json --root .
python -B -X utf8 plugins/nova-the-optimal-ai/skills/software-verification/scripts/validate_traceability.py verification/testforge-free-v3/operator/verification-manifest.json
```

Next, obtain the six grants, reconcile the two metadata sets, rebuild from the resulting clean checkpoint, rerun license/Hesperos/TestForge/package gates, execute fresh-host journeys, and submit the new stable packet to independent TestForge review. Hosted CI remains on hold until a fresh authoritative capacity snapshot and applicable authority exist.

## Authority still required

Separate principal authority is required for rights grants, metadata reconciliation where it changes licensed package claims, clean-host resources, any paid hosted verification, and every push, tag, GitHub release, Pages deployment, directory submission, customer distribution, or announcement. The build request authorized none of those external actions.