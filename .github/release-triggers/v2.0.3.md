# Nova + MIND Free 2.0.3 release trigger

Disposable pull-request marker used only to invoke the owner-scoped release workflow against `main`. This file is not intended to be merged.

Synchronization requested after the protected release trigger was added to `main`.
