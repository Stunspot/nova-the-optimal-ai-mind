# Nova the Optimal AI + MIND site source

The static project site is published from this `docs/` directory.

## Source and evidence boundary

The site describes the public OpenAI Build Week contest release in this repository. Its product claims are derived from:

- `README.md`, `START-HERE.md`, `BUILD-WEEK-CONTRIBUTION.md`, and the public demo;
- `plugins/nova-the-optimal-ai/skills/nova/SKILL.md` and its routing, tour, memory, maker-context, and brand references;
- `plugins/augment-of-mind/skills/augment-of-mind/SKILL.md`, the Faculty runtime, registry, and public MIND assets;
- the included specialist skills, plugin manifests, release archives, verification report, and traceability matrix.

The page does not claim that all 28 skill handles must be operated by the user, that MIND is active when not installed, that a specialist expands user authority, that durable memory exists without a persistence receipt, or that untested hosts and operating systems share the exercised contest evidence.

## Files

- `index.html` — semantic single-page product and capability-ecology overview;
- `style.css` — responsive presentation and accessibility treatment;
- `assets/nova-emergent.png` — exact copied canonical Nova raster artwork from `assets/nova-emergent.png`;
- `.nojekyll` — direct static-file serving marker.

## Deployment

`.github/workflows/deploy-pages.yml` uploads this directory with GitHub's official Pages Actions. Repository Pages must be configured to use **GitHub Actions** before the first deployment can publish.

## Review notes

The page uses one H1, semantic landmarks, a skip link, visible keyboard focus, descriptive links, meaningful alternative text, responsive layout, and reduced-motion handling. The canonical Nova artwork is copied byte-for-byte rather than redrawn, recolored, or converted to SVG. These checks support structural accessibility only; they are not a claim of formal accessibility conformance, host compatibility, installation success outside the exercised path, or representative-user success.
