# Codex installation

Use the complete customer package and the local marketplace under its codex directory.

    codex plugin marketplace add "<extracted package>/codex" --json
    codex plugin add nova-the-optimal-ai --marketplace collaborative-dynamics-nova-free --json
    codex plugin list --json

Inspect before replacing another governing Nova installation. Start a new task after catalog changes. Confirm one real Nova invocation and distinguish package presence, installation, discovery, enabled state, invocation, and behavior.

No prompt hook, Ollama model, embedding service, vector database, or persistent estate is required. Configure state later only at the customer's request through Nova Operations.
